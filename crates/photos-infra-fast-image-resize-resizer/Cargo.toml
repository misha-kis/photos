[package]
name = "photos-infra-fast-image-resize-resizer"
version = "0.1.0"
edition = "2024"

[dependencies]
fast_image_resize = { version = "5.4.0", features = ["image"] }
image.workspace = true
photos-services = { path = "../photos-services" }

